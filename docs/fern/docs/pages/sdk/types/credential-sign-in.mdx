---
title: CredentialSignIn
---

# CredentialSignIn

A component that renders a form for signing in with email and password credentials.

## Props

This component does not accept any props.

## Example

```tsx
import { CredentialSignIn } from '@stackframe/stack';

function SignInPage() {
  return (
    <div>
      <h1>Sign In</h1>
      <CredentialSignIn />
    </div>
  );
}
```
